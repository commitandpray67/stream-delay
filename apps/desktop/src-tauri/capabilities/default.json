{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The dashboard window loads the local web UI over HTTP and needs no Tauri commands.",
  "windows": ["main"],
  "permissions": []
}
